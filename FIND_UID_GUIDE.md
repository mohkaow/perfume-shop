# 🔍 วิธีการหา Firebase UID

## วิธีที่ 1: Firebase Console UI (ง่ายที่สุด)

### ขั้นตอน:
1. ไปที่ https://console.firebase.google.com
2. เลือก Project: **perfume-shop-82ac7**
3. คลิก **Authentication** (ด้านซ้าย)
4. คลิกแท็บ **Users**
5. หา Email ที่ต้องการ
6. ยิมูส email นั้นเพื่อดูรายละเอียด
7. **UID** จะแสดงอยู่ด้านบน
8. **คลิก Copy Icon** เพื่อคัดลอก

---

## วิธีที่ 2: Firebase Admin Console (ถ้ามีสิทธิ์)

หากมี Firebase Admin Access:

```bash
# ใช้ Firebase CLI
firebase auth:export data.json --project perfume-shop-82ac7

# แล้วหา UID ใน data.json
```

---

## วิธีที่ 3: JavaScript Console (ในแอป)

ถ้าคุณ login ด้วยบัญชี Admin:

```javascript
// ไปที่ Browser Console (F12 → Console)
// แล้วรัน:

import { getAuth } from 'firebase/auth';
const auth = getAuth();
const user = auth.currentUser;
console.log('Current User UID:', user?.uid);
```

---

## 📋 UID Format

- **ความยาว**: 28 ตัวอักษร
- **ตัวอักษร**: A-Z, a-z, 0-9
- **ตัวอย่าง**: 
  ```
  k8mP2qL9xN4vQ6rS1tU3w5yZ7aB9cD2e4F6gH8jK
  ```

---

## ⚠️ สิ่งที่ต้องระวัง

- ✅ UID จะมีตัวพิมพ์เล็กและใหญ่ คัดลอกให้ถูกต้อง
- ✅ ไม่มี dash (-) หรือขีดเส้นใหญ่ (_)
- ✅ คัดลอกทั้งหมด (ไม่ต้องสรุป)
- ✅ ใช้ UID นี้เป็น Document ID ใน Firestore

---

## 🎯 เมื่อได้ UID แล้ว

1. **ไปที่ Firestore**
   - Database → admins Collection

2. **สร้าง Document ใหม่**
   - Document ID: วางค่า UID ที่คัดลอก
   - เพิ่ม Fields:
     - email: (email ของ admin)
     - name: (ชื่อ admin)
     - role: "admin"
     - isActive: true

3. **บันทึก**

---

**Tips**: ถ้าลืม UID ของ Admin ที่มีอยู่
- ไปที่ Firestore → admins Collection
- ดู Document IDs ที่สร้างไว้ (นั่นคือ UIDs)

