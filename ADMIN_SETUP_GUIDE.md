# 🔐 การเพิ่ม Admin ใหม่ - คำแนะนำทั้งหมด

## วิธีการเพิ่ม Admin ใหม่

### ขั้นตอนที่ 1: สร้าง Account ใน Firebase Console

1. **เข้า Firebase Console**
   - ไปที่ https://console.firebase.google.com
   - เลือก Project: **perfume-shop-82ac7**

2. **ไปที่ Authentication**
   - ในเมนูด้านซ้าย คลิก **Authentication**
   - คลิกแท็บ **Users**

3. **สร้าง User ใหม่**
   - คลิกปุ่ม **Create User** (สีน้ำเงิน)
   - กรอกข้อมูล:
     ```
     Email: admin@example.com (เปลี่ยน email ให้เหมาะสม)
     Password: ตั้ง password ที่ยาว (อย่างน้อย 6 ตัวอักษร)
     ```
   - คลิก **Create User**

### ขั้นตอนที่ 2: ตั้งค่า Admin Role ใน Firestore

1. **ไปที่ Firestore Database**
   - ในเมนูด้านซ้าย คลิก **Firestore Database**

2. **สร้าง Collection ชื่อ "admins" (ถ้ายังไม่มี)**
   - คลิก **Create Collection**
   - ชื่อ Collection: `admins`
   - Document ID: ใช้ UUID หรือ `auto`

3. **เพิ่ม Admin Document**
   - ใน Collection `admins` คลิก **Add Document**
   - Document ID: **ใช้ User ID ที่สร้างจาก Firebase Auth**
     - (หาได้จาก Firebase Console → Authentication → Users → คัดลอก UID)
   
   - เพิ่ม Fields:
     ```
     email (String): admin@example.com
     name (String): ชื่อ Admin
     role (String): admin
     createdAt (Timestamp): วันที่สร้าง
     isActive (Boolean): true
     ```

### ขั้นตอนที่ 3: ตรวจสอบ Firestore Rules

ตรวจสอบให้แน่ใจว่า Firestore Rules มีสิทธิ์อ่าน admins collection:

```javascript
// ใน Firestore Rules ต้องมีบรรทัดนี้
match /admins/{document=**} {
  allow read: if true;  // อนุญาติให้อ่านข้อมูล admin
  allow write: if false; // ไม่อนุญาติให้เขียน (ควบคุมจาก backend)
}
```

## 📋 ข้อมูลที่ต้องการสำหรับ Admin ใหม่

### บน Firebase
- ✅ Email
- ✅ Password
- ✅ User ID (Firebase UID)
- ✅ Full Name
- ✅ Role: "admin"
- ✅ Is Active: true

### ตัวอย่าง Firestore Document
```json
{
  "email": "admin@perfume-shop.com",
  "name": "นาย Admin",
  "role": "admin",
  "createdAt": "2025-11-28",
  "isActive": true,
  "phone": "0812345678",
  "notes": "Admin หลัก"
}
```

## 🔑 Credentials สำหรับ Admin ปัจจุบัน

**ตัวอย่าง Admin ที่มีอยู่:**
```
Email: admin@perfume-shop.com
Password: (ตั้งใจไว้เป็นส่วนตัว)
Role: admin
```

## 🚀 วิธีทดสอบ Admin ใหม่

1. **ไปที่ Admin Login Page**
   - URL: `http://localhost:5174/admin/login`
   - หรือใน Production: `https://perfume-shop-eta.vercel.app/admin/login`

2. **ลองเข้าระบบ**
   - Email: อีเมลของ Admin ใหม่
   - Password: password ที่สร้าง

3. **ตรวจสอบการเข้าถึง**
   - ถ้าเข้าได้จะเห็นหน้า Admin Dashboard
   - ถ้าไม่เข้าจะกลับไป Login Page

## 🔒 Security Checklist

- ✅ ใช้ Email ที่จริง (สำหรับการติดต่อ)
- ✅ ตั้ง Password ที่ยาวและปลอดภัย
- ✅ อย่าแชร์ Password กับใครเลย
- ✅ เปลี่ยน Password เป็นระยะๆ
- ✅ ตั้ง Admin ใหม่เฉพาะคนที่เชื่อถือได้
- ✅ สามารถปิด Admin ได้ด้วยการตั้ง `isActive: false`

## 🚫 การปิด/ลบ Admin

### วิธี 1: ปิด Admin (แนะนำ)
1. ไปที่ Firestore → admins collection
2. แก้ไข Document ของ Admin นั้น
3. เปลี่ยน `isActive` เป็น `false`
4. Admin นั้นจะไม่สามารถเข้าระบบได้

### วิธี 2: ลบ Admin ออกจากระบบ
1. **ใน Firebase Authentication**
   - ไปที่ Authentication → Users
   - หาผู้ใช้ที่ต้องการลบ
   - คลิก 3 dots → Delete User

2. **ใน Firestore**
   - ไปที่ admins collection
   - ลบ Document ของ Admin นั้น

## 📞 ติดต่อ Support

หากมีปัญหาในการเพิ่ม Admin:

1. ตรวจสอบ Firestore Rules ให้แน่ใจว่าเปิดให้อ่าน admins
2. ตรวจสอบว่า User ID ตรงกับ Document ID ใน Firestore
3. ตรวจสอบ Email ให้แน่ใจว่าถูกต้อง
4. ลองเข้า Firebase Console ทดสอบ

## 🔗 Links ที่มีประโยชน์

- 📱 **Firebase Console**: https://console.firebase.google.com
- 🌐 **Admin Login**: http://localhost:5174/admin/login
- 📦 **Firestore Database**: https://console.firebase.google.com/project/perfume-shop-82ac7/firestore
- 👥 **Authentication**: https://console.firebase.google.com/project/perfume-shop-82ac7/authentication

---

## ⚠️ สิ่งที่ต้องจำ

| ข้อมูล | ที่เก็บ | สิทธิ์ |
|------|-------|-------|
| Email | Firebase Auth | เข้าระบบ |
| Password | Firebase Auth | เข้าระบบ |
| Role | Firestore (admins) | ตรวจสอบสิทธิ์ |
| User ID | Firebase Auth | เชื่อมต่อข้อมูล |

---

**Last Updated**: November 28, 2025  
**Version**: 1.0.0
